name: Run tests with workflow_call

on: 
  workflow_dispatch:
    inputs:
      package:
        description: "Package to test"
        required: true
        type: choice
        options:
          - qpay-customer-ui
          - qpay-ui

  schedule:
    - cron: 35 13 * * *
    - cron: 40 13 * * *

jobs:
  test:
    name: "Run E2E tests"
    uses: ./.github/workflows/mochawesome.yaml
    with:
      package-name: |
        "${{ inputs.package 
            || (github.event.schedule == '40 13 * * *' && 'qpay-ui') 
            || (github.event.schedule == '35 13 * * *' && 'qpay-customer-ui') }}"

